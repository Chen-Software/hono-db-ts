# dependencies (bun install)
node_modules

# output
out
dist
*.tgz

# generated model plans (regenerated by `models:build` / `db:generate` / `db:migrate`)
src/generated/models.json

# generated migration SQL (regenerated by `db:generate` / `cf-build`; bundled into
# the worker by `scripts/cf-build.ts`)
drizzle/

# generated Cloudflare Worker config (regenerated from `wrangler.config.ts`)
wrangler.jsonc

# local dev database
*.db
*.db-journal

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# prod
dist/

# dev
.hono/
.wrangler/
.yarn/
!.yarn/releases
.vscode/*
!.vscode/launch.json
!.vscode/*.code-snippets
.idea/workspace.xml
.idea/usage.statistics.xml
.idea/shelf

# logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# misc
.DS_Store

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

design-system
